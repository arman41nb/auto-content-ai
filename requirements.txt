requests
python-dotenv
pydantic
Pillow
edge-tts
imageio-ffmpeg

# Optional: enables OCR-based accidental text detection if Tesseract is installed.
# pytesseract
#
# Optional fallback TTS library; not required by the current CLI.
# pyttsx3
